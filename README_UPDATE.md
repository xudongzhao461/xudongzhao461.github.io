# Homepage update package

The enclosed `index.html` is self-contained.

- The supplied portrait is embedded directly in the HTML, so no separate image upload is needed.
- Replace the repository-root `index.html` with this file.
- A branch named `update-profile-2026` has already been prepared.
- Suggested commit message: `Refresh academic homepage from 2026 CV`
- After review, merge the branch into `main`; GitHub Pages should redeploy automatically.

Privacy choice: the public page omits date of birth, native place, and telephone number.  
Verification choice: known publisher links are used directly; newer entries without a stable public record use title-search links rather than an invented DOI.
